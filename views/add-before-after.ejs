<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New Before & After Case</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/admin/menu">Admin Menu</a>
        </div>
    </nav>

    <div class="container my-5">
        <h1 class="mb-4 text-center fw-bold">Add New Before & After Case</h1>

        <div class="card shadow-lg p-4">
            <form action="/admin/add-before-after" method="POST">
                <div class="mb-3">
                    <label class="form-label">Title</label>
                    <input type="text" class="form-control" name="title" placeholder="Enter case title" required>
                </div>

                <div class="mb-3">
                    <label class="form-label">Description</label>
                    <textarea class="form-control" name="description" placeholder="Enter case description" rows="3" required></textarea>
                </div>

                <div class="mb-3">
                    <label class="form-label">Before Image URL</label>
                    <input type="text" class="form-control" name="before_image" placeholder="Paste Before Image URL" required>
                </div>

                <div class="mb-3">
                    <label class="form-label">After Image URL</label>
                    <input type="text" class="form-control" name="after_image" placeholder="Paste After Image URL" required>
                </div>

                <h5 class="fw-bold mt-4">Extra Images (Optional, up to 10)</h5>
                <div class="row g-2">
                    <% for (let i = 0; i < 10; i++) { %>
                        <div class="col-md-4">
                            <input type="text" class="form-control" name="extra_images[]" placeholder="Extra Image URL">
                        </div>
                    <% } %>
                </div>

                <div class="text-center mt-4">
                    <button type="submit" class="btn btn-primary btn-lg">Add Case</button>
                    <a href="/admin/edit-before-after" class="btn btn-secondary btn-lg">Cancel</a>
                </div>
            </form>
        </div>
    </div>

    <!-- ✅ Bootstrap JS (Always place before closing `</body>` tag) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
